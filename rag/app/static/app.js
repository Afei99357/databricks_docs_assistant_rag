const q=document.querySelector('#question'),result=document.querySelector('#result'),answer=document.querySelector('#answer'),sources=document.querySelector('#sources'),references=document.querySelector('#references'),referenceSummary=document.querySelector('#reference-summary'),loading=document.querySelector('#loading'),ask=document.querySelector('#ask'),starters=[...document.querySelectorAll('.starter')],transcript=document.querySelector('#transcript'),historyPanel=document.querySelector('#history-panel'),historyToggle=document.querySelector('#history-toggle');let latest={},conversationId=sessionStorage.getItem('rag-conversation-id')||'';
const escapeHtml=value=>String(value).replace(/[&<>'"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]));
const renderAnswer=text=>escapeHtml(text).replace(/\[(S\d+)\]/g,'<a class="citation" href="#source-$1">[$1]</a>').split('\n').filter(line=>line.trim()).map(line=>`<p>${line}</p>`).join('');
const addTurn=(role,text)=>{const el=document.createElement('article');el.className=`turn ${role}`;el.innerHTML=`<strong>${role==='user'?'You':'Assistant'}</strong><div></div>`;el.querySelector('div').textContent=text;transcript.append(el)};
function showAnswer(data){latest=data;answer.innerHTML=renderAnswer(data.answer);sources.textContent='';(data.citations||[]).forEach(c=>{const el=document.createElement('article');el.id=`source-${c.label}`;el.innerHTML=`<a target="_blank" rel="noopener" href="${c.url}">${c.label}: ${escapeHtml(c.title)}</a><p class="url"></p><p class="excerpt"></p>`;el.querySelector('.url').textContent=c.url;el.querySelector('.excerpt').textContent=c.excerpt;sources.append(el)});const count=(data.citations||[]).length;referenceSummary.textContent=`Official references (${count} source${count===1?'':'s'})`;references.hidden=!count;references.open=false;result.hidden=false}
function busy(value){loading.hidden=!value;ask.disabled=value;q.disabled=value;starters.forEach(x=>x.disabled=value)}
function setConversation(id){conversationId=id||'';if(conversationId)sessionStorage.setItem('rag-conversation-id',conversationId);else sessionStorage.removeItem('rag-conversation-id')}
starters.forEach(b=>b.onclick=()=>{q.value=b.textContent;q.focus()});
document.querySelector('#new-conversation').onclick=()=>{setConversation('');transcript.textContent='';result.hidden=true;q.value='';q.focus()};
async function loadTurns(id){const r=await fetch(`/api/conversations/${encodeURIComponent(id)}/turns`);const body=await r.json();if(!r.ok)throw new Error(body.error||'Could not load conversation.');setConversation(id);transcript.textContent='';for(const turn of body.turns||[]){addTurn('user',turn.question);addTurn('assistant',turn.answer)}result.hidden=true;historyPanel.hidden=true;historyToggle.setAttribute('aria-expanded','false');q.focus()}
function renderHistory(rows){historyPanel.textContent='';if(!rows.length){historyPanel.textContent='No past conversations yet.';return}rows.forEach(row=>{const b=document.createElement('button');b.type='button';b.className='history-item';b.textContent=row.title;b.onclick=()=>loadTurns(row.conversation_id).catch(e=>{historyPanel.textContent=e.message});historyPanel.append(b)})}
async function toggleHistory(){const opening=historyPanel.hidden;historyPanel.hidden=!opening;historyToggle.setAttribute('aria-expanded',String(opening));if(!opening)return;historyPanel.textContent='Loading…';try{const r=await fetch('/api/conversations'),body=await r.json();if(!r.ok)throw new Error(body.error||'Could not load history.');renderHistory(body.conversations||[])}catch(e){historyPanel.textContent=e.message}}
historyToggle.onclick=toggleHistory;
document.querySelector('#question-form').onsubmit=async e=>{e.preventDefault();const question=q.value.trim();if(!question)return;busy(true);q.value='';addTurn('user',question);try{const r=await fetch('/api/answer',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({question,conversation_id:conversationId||undefined})}),body=await r.json();if(!r.ok||body.error)throw new Error(body.error||'The request could not be completed.');setConversation(body.conversation_id);addTurn('assistant',body.answer);showAnswer(body)}catch(e){addTurn('assistant',e.message)}finally{busy(false);q.focus()}};
for(const [id,rating] of [['up','up'],['down','down']])document.querySelector('#'+id).onclick=()=>fetch('/api/feedback',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({rating,question:latest.question,snapshot_id:latest.snapshot_id,retrieved_chunk_ids:latest.retrieved_chunk_ids,latency_ms:latest.latency_ms,comment:document.querySelector('#comment').value})});
